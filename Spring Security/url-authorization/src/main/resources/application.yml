spring:
  application:
    name: url-authorization-demo

  # H2 ???? ??????
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:

  # H2 ?? ???
  h2:
    console:
      enabled: true
      path: /h2-console

  # JPA ??
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  # Thymeleaf ??
  thymeleaf:
    cache: false

# ?? ??
server:
  port: 8080

# ?? ??
logging:
  level:
    com.example.security: DEBUG
    org.springframework.security: DEBUG